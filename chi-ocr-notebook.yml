version: "2"

services:
  notebook:
    build: ./chi-ocr-docker/
    image: chi-ocr-notebook
    container_name: ${NAME}
    volumes:
     - "./chi-ocr-work:/home/jovyan/work"
    ports:
      - "${PORT}:8888"

#volumes:
#  work:
#    external:
#      name: ${WORK_VOLUME}